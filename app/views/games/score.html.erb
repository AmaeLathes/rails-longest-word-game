<form action="TODO" method="TODO" data-turbo="false">
<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<!-- app/views/games/score.html.erb -->
<div class="container">
  <h1>Game Result</h1>
  <p><%= @message %></p>
  <% if @included && @valid_english_word %>
    <p>Your word <%= @word %> scored <%= @score %> points.</p>
    <p>Total score: <%= session[:score] %></p>
  <% end %>
  <%= link_to 'Play Again', new_game_path, class: 'btn btn-primary' %>
</div>
  </form>
